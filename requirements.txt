requests>=2.32
tenacity>=9.0
pytest>=8.0

# Los cargadores de datos del tablero (dashboard/data/*.csv.py) leen los
# CSV procesados con pandas y los sirven a Observable Framework. pyarrow
# ya no es obligatorio: la salida es CSV, no parquet.
pandas>=2.2
